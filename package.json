{
  "name": "rollemtech-sst3",
  "version": "1.0.1",
  "private": true,
  "scripts": {
    "dev": "sst dev",
    "cdk:bootstrap": "npx cdk bootstrap",
    "build": "sst build",
    "check": "turbo build lint format typecheck",
    "clean": "git clean -xdf node_modules .sst .turbo",
    "clean:sst": "git clean -xdf .sst",
    "clean:workspaces": "turbo clean",
    "console": "sst console",
    "deploy": "pnpm with-env sst deploy",
    "down": "sst remove",
    "format": "turbo format --continue -- --cache --cache-location='node_modules/.cache/.prettiercache'",
    "format:fix": "turbo format --continue -- --write . --cache --cache-location='node_modules/.cache/.prettiercache'",
    "gen": "turbo gen init",
    "lint": "turbo lint --continue -- --cache --cache-location 'node_modules/.cache/.eslintcache' && manypkg check",
    "lint:fix": "turbo lint --continue -- --fix --cache --cache-location 'node_modules/.cache/.eslintcache' && manypkg fix",
    "secrets": "ts-node ./secrets.ts",
    "test": "turbo test",
    "test:withenv": "pnpm with-env turbo test",
    "typecheck": "turbo typecheck",
    "up:workspaces": "pnpm up -r --workspace",
    "with-env": "dotenv -e .env --"
  },
  "dependencies": {
    "@acme/prettier-config": "^0.1.0",
    "@manypkg/cli": "^0.21.1",
    "@tsconfig/node18": "^18.2.2",
    "@turbo/gen": "^1.11.3",
    "aws-cdk-lib": "2.95.1",
    "constructs": "10.2.69",
    "dotenv-cli": "^7.3.0",
    "prettier": "^3.2.1",
    "sst": "^2.39.5",
    "turbo": "^1.11.3",
    "typescript": "^5.3.3"
  },
  "workspaces": [
    "apps/*",
    "functions/*",
    "packages/*",
    "tooling/*"
  ]
}
