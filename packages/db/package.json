{
  "name": "@acme/db",
  "version": "0.1.0",
  "private": true,
  "main": "./index.ts",
  "types": "./index.ts",
  "license": "UNLICENSED",
  "scripts": {
    "clean": "git clean -xdf .turbo node_modules",
    "lint": "eslint .",
    "format": "prettier --check \"**/*.{mjs,ts,md,json}\"",
    "check": "drizzle-kit check:pg",
    "generate": "drizzle-kit generate --dialect=pg --schema=schema.ts --out=migrations",
    "push": "drizzle-kit push:pg --driver=mysql2 --schema=schema.ts",
    "studio": "drizzle-kit studio --port 1337",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@aws-sdk/credential-providers": "^3.431.0",
    "drizzle-orm": "^0.28.6",
    "sst": "^2.29.2"
  },
  "devDependencies": {
    "@acme/eslint-config": "0.2.0",
    "@acme/prettier-config": "^0.1.0",
    "@acme/tsconfig": "^0.1.0",
    "@aws-sdk/client-rds-data": "^3.431.0",
    "@types/node": "^18.16.16",
    "@types/pg": "^8.10.7",
    "drizzle-kit": "^0.19.13",
    "eslint": "^8.50.0",
    "typescript": "^5.2.2"
  },
  "eslintConfig": {
    "extends": [
      "@acme/eslint-config/base"
    ]
  },
  "prettier": "@acme/prettier-config"
}
